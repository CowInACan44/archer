extends CanvasLayer

## Reserved for usable items (bombs, etc.) and a future RTS-style unit
## select tool - not wired to anything yet. Tower actions live in the
## Stats tab instead, since they need to work per-tower rather than as a
## single global "use nearest tower" button.


func _ready() -> void:
	pass
